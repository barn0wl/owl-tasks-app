export enum Priority {
    None = 0,
    Low,
    Mid,
    High
}

export enum Weekday {
    Sunday = 0,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}

export enum RecurrenceInterval {
    None = 'N/A',
    Daily = 'Daily',
    Weekly = 'Weekly',
    Monthly = 'Monthly'
}