export enum Priority {
    None = 0,
    Low,
    Mid,
    High
}

export enum Status {
    NotStarted = 'Not started',
    InProgress = 'In progress',
    Complete = 'Complete'
}

export enum Weekday {
    Sunday = 0,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}

export enum RecurrenceInterval {
    None = 'N/A',
    Daily = 'Daily',
    Weekly = 'Weekly',
    Monthly = 'Monthly'
}